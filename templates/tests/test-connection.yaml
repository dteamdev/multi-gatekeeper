apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "multi-gatekeeper.fullname" . }}-test-connection"
  labels:
    {{- include "multi-gatekeeper.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "multi-gatekeeper.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
