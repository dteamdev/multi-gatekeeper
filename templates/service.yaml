apiVersion: v1
kind: Service
metadata:
  name: {{ include "multi-gatekeeper.fullname" . }}
  labels:
    {{- include "multi-gatekeeper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "multi-gatekeeper.selectorLabels" . | nindent 4 }}
